export interface IPageInfo {
    // Misspelled at source.
    currnet_page_number: number;
    // Misspelled at source.
    currnet_page_size: number;
    total_page_count: number;
    total_record_count: number;
}
